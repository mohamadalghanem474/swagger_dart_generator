# Swagger Dart Generator

A powerful Dart code generator that creates complete Flutter project structure from Swagger/OpenAPI JSON files.

## Features

- Generates models, datasources, repositories, controllers, and endpoints
- Creates integration tests
- Supports Dio for HTTP requests
- Uses injectable for dependency injection

## Installation

```bash
dart pub global activate swagger_dart_generator